<%= form_with(model: order_content, local: true) do |form| %>
  <% if order_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_content.errors.count, "error") %> prohibited this order_content from being saved:</h2>

      <ul>
      <% order_content.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :count %>
    <%= form.text_field :count %>
  </div>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id %>
  </div>

  <div class="field">
    <%= form.label :menu_id %>
    <%= form.text_field :menu_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
